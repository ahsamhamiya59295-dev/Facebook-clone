# Same-origin is the default (served by the backend in single-server deploys).
# Set these ONLY when the API/Socket host differs from the frontend host.
VITE_API_URL=/api
VITE_SOCKET_URL=
VITE_APP_NAME=ornaConnect
